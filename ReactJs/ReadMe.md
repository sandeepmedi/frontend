1. Create the ProjectRun the following command in your terminal. Replace my-react-app with your preferred project name:bash
npm create vite@latest my-react-app-01 -- --template react

2. Install Dependencies & StartNavigate into your project folder, install the required packages, and launch the local development server:

cd my-react-app
npm install
npm run dev